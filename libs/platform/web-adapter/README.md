# web-adapter

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build web-adapter` to build the library.

## Running unit tests

Run `nx test web-adapter` to execute the unit tests via [Vitest](https://vitest.dev/).
